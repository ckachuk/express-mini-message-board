extends layout

block content
  h1= title
  p Welcome to #{title}
  ul 
    each message in messages
      li= message.user 
      p= message.text
      p= message.added
    else 
      li There are no message 
